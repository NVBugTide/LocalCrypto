# LocalVault
LocalVault is a small client-side offline tool for encrypting and decrypting files on your own machine. Drop a file, choose the password, and download an encrypted file or a self-decrypting HTML to decrypt in local browser.
